import React from 'react';
import {Container} from "@material-ui/core";

const Employer = () => {
    return (
        <Container>
            Employer
        </Container>
    );
};

export default Employer;