import React from 'react';
import {Container} from "@material-ui/core";

const HomePage = () => {
    return (
        <Container>
            HomePage
        </Container>
    );
};

export default HomePage;