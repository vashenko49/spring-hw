import React from 'react';
import {Container} from "@material-ui/core";

const Account = () => {
    return (
        <Container>
            Account
        </Container>
    );
};

export default Account;